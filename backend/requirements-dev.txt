-r requirements.txt
# pytest 9.1.1: resolves PYSEC-2026-1845 (fixed in 9.0.3+) — see
# docs/ACCESS_AND_TESTING.md Phase 6 Chunk K.
pytest==9.1.1
# pytest-django 4.9.0 (the previous pin) never declared Django 5.2 support;
# 4.11.0 added it. Bumped to 4.11.0 specifically (not the latest 4.14.0) —
# the Django 5.2 upgrade re-verified this version against pytest==9.1.1
# directly (full suite run, see docs/ACCESS_AND_TESTING.md); the
# 4.13.0-vs-pytest-9 incompatibility noted previously
# ('PytestDjangoTestCase' has no attribute '_pre_setup_ran_eagerly') was
# never fixed upstream in the pytest-django changelog, so later versions
# aren't assumed safe without the same re-verification.
pytest-django==4.11.0
ruff==0.9.4
